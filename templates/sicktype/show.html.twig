{% extends 'base.html.twig' %}

{% block title %}Sicktype{% endblock %}

{% block body %}
    <h1>Sicktype</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sicktype.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sicktype.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sicktype_index') }}">back to list</a>

    <a href="{{ path('sicktype_edit', {'id': sicktype.id}) }}">edit</a>

    {{ include('sicktype/_delete_form.html.twig') }}
{% endblock %}
