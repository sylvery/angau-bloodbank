{% extends 'base.html.twig' %}

{% block title %}Edit Donor{% endblock %}

{% block body %}
    <h1>Edit Donor</h1>

    {{ include('donor/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('donor_index') }}">back to list</a>

    {{ include('donor/_delete_form.html.twig') }}
{% endblock %}
