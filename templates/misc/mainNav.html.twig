<nav class="navbar navbar-expand-sm navbar-dark bg-dark">
    <a class="navbar-brand" href="{{path('home')}}">BloodBank</a>
    <button class="navbar-toggler d-lg-none" type="button" data-toggle="collapse" data-target="#mobilMenuNav" aria-controls="mobilMenuNav"
        aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="mobilMenuNav">
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
            <li class="nav-item active">
                <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('donation_index')}}">Donation</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('donor_index')}}">Donors</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('location_index')}}">Locations</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('sicktype_index')}}">Sick Types</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('employee_index')}}">Employees</a>
            </li>
        </ul>
        <form class="form-inline my-2 my-lg-0">
            <input class="form-control mr-sm-1" type="text" placeholder="Search">
            <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
        </form>
        <nav class="nav justify-content-end">
            {% if app %}
            <a class="btn btn-danger btn-sm nav-link" role="button" href="{{path('app_login')}}">Login</a>
            {% else %}
            <a class="btn btn-danger btn-sm nav-link" role="button" href="{{path('app_logout')}}">Sign Out</a>
            {% endif %}
        </nav>
    </div>
</nav>